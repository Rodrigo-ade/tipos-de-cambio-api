## Instalación: 
- `npm install`
## Comandos: 
- `abrir-servidor` para abrir el servidor (necesario para testear con cypress)
* `abrir-cypress-terminal` para abrir cypress sin interfaz
+ `abrir-cypress` para abrir cypress 
